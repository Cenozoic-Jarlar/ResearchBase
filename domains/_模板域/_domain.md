# 域配置：模板域（复制本目录改名为你的域，例如"工作A"）

域简介：
默认价值观：
风格偏好：

---
说明：本文件是域的可选自描述配置（与主题库 _repo.md 同构，人工/AI 均可读）。
「默认价值观」填价值观框架名（逗号分隔，可留空），如：policy, technology。
任务在该域运行时：未显式选择价值观 → 自动用域默认 → 再回退关键词自动匹配 / general。
框架库在 memory/profiles/（全局单份，不按域复制）。
